Rotate these secrets now (treat previous values as compromised):
- OPENAI_API_KEY
- SMTP_* / EMAIL_SERVER_* / EMAIL_FROM
- NEXTAUTH_SECRET / AUTH_SECRET
- ADMIN_API_TOKEN
- DATABASE_URL (if credentials embedded)
Then update .env with new values locally and in your secret store.
