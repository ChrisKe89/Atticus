{
  "name": "atticus-web",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend tooling for the Atticus UI (TailwindCSS build pipeline).",
  "scripts": {
    "tailwind:build": "tailwindcss -c tailwind.config.js -i web/static/css/tailwind.css -o web/static/css/style.css",
    "tailwind:watch": "npm run tailwind:build -- --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.40",
    "tailwindcss": "^3.4.10"
  },
  "dependencies": {
    "jsdom": "^27.0.0"
  }
}
